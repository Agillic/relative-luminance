import {getLightnessFromHexColor, isColorDark, isColorBright} from './relativeLuminance'

const relativeLuminanceFunctions = {
  getLightnessFromHexColor,
  isColorDark,
  isColorBright
}

export {
  getLightnessFromHexColor,
  isColorDark,
  isColorBright
}

export default relativeLuminanceFunctions
